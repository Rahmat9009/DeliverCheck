# Canonical public HTTPS origin used in discovery documents.
PUBLIC_BASE_URL=

# Optional fail-closed SharedOS Cloud audit export. Leave both unset until an
# official HTTPS ingestion endpoint and key have been provisioned.
SHAREDOS_KEY=
SHAREDOS_AUDIT_URL=
