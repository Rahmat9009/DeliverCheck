{
  "request_id": "req-000123",
  "input_format": "json",
  "source_text": "{\"invoice_id\":123,\"currency\":\"QAR\",\"amount\":\"17.50\"}",
  "target_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "required": ["invoice_id", "currency", "amount"],
    "properties": {
      "invoice_id": { "type": "string", "pattern": "^[0-9]{6}$" },
      "currency": { "const": "QAR" },
      "amount": { "type": "string", "pattern": "^[0-9]+\\.[0-9]{2}$" }
    }
  },
  "explicit_rules": [
    "invoice_id is a six-character identifier and meaningful leading zeros must be preserved.",
    "currency must be QAR.",
    "amount is a decimal string with exactly two fractional digits."
  ],
  "downstream_error": "invoice_id must be a string matching ^[0-9]{6}$"
}
